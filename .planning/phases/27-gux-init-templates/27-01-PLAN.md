---
phase: 27-gux-init-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/gux/templates/app.go.tmpl
  - cmd/gux/templates/go.mod.tmpl
  - cmd/gux/templates/models/item.go.tmpl
autonomous: true

must_haves:
  truths:
    - "app.go.tmpl renders valid Go code with core.New(), SetDB(), CRUD(), Routes(), Run()"
    - "go.mod.tmpl includes gux, gorm, and sqlite dependencies"
    - "models/item.go.tmpl defines GORM model with ID, Name, Description, timestamps"
  artifacts:
    - path: "cmd/gux/templates/app.go.tmpl"
      provides: "Main app template using core/ framework"
      contains: "core.New()"
    - path: "cmd/gux/templates/go.mod.tmpl"
      provides: "Module file template with dependencies"
      contains: "gorm.io/gorm"
    - path: "cmd/gux/templates/models/item.go.tmpl"
      provides: "Example GORM model template"
      contains: "gorm.Model"
  key_links:
    - from: "cmd/gux/templates/app.go.tmpl"
      to: "models/item.go"
      via: "import and CRUD registration"
      pattern: "app\\.CRUD\\(models\\.Item"
---

<objective>
Create the core scaffold templates (app.go, go.mod, models/item.go) that produce a working Gux application using the modern core/ framework pattern.

Purpose: Replace the outdated cmd/app + cmd/server architecture with a single app.go using hybrid SSR+WASM routing
Output: Three template files in cmd/gux/templates/ that scaffold the foundation of a new Gux app
</objective>

<execution_context>
@/Users/dougbarrett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dougbarrett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-gux-init-templates/27-RESEARCH.md

# Reference implementation
@examples/minimal/app.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app.go.tmpl template</name>
  <files>cmd/gux/templates/app.go.tmpl</files>
  <action>
Create the main application template at cmd/gux/templates/app.go.tmpl.

The template must:
1. Use package main
2. Import: log, github.com/dougbarrett/gux/core, {{.ModulePath}}/.gux/api, {{.ModulePath}}/models, {{.ModulePath}}/pages, gorm.io/driver/sqlite, gorm.io/gorm
3. In main():
   - Open sqlite database "app.db" with gorm.Open()
   - Call db.AutoMigrate(&models.Item{})
   - Call api.Init(db) to initialize generated API
   - Create app with core.New()
   - Call app.SetDB(db)
   - Call app.SetTitle("{{.AppName}}")
   - Register CRUD: app.CRUD(models.Item{})
   - Register routes: app.Routes().Hybrid("/", pages.Home).Hybrid("/items", pages.Items).Hybrid("/items/new", pages.ItemNew)
   - Call app.Run(":8080")

Use {{.ModulePath}} for import paths (will be substituted by scaffold.go).
Use {{.AppName}} for the title.

IMPORTANT: Do NOT use backticks in template variable substitution. Use the direct form {{.FieldName}}.
  </action>
  <verify>
cat cmd/gux/templates/app.go.tmpl | grep -E "(core\.New|SetDB|CRUD|Routes|Hybrid|Run)"
  </verify>
  <done>
app.go.tmpl exists with core.New(), SetDB(), CRUD(), Routes(), Hybrid routes, and Run() calls
  </done>
</task>

<task type="auto">
  <name>Task 2: Update go.mod.tmpl template</name>
  <files>cmd/gux/templates/go.mod.tmpl</files>
  <action>
Update the go.mod template at cmd/gux/templates/go.mod.tmpl.

The template must:
1. Use module {{.ModulePath}}
2. Use go 1.24
3. Include require block with:
   - {{.GuxModule}} {{.GuxVersion}}
   - gorm.io/driver/sqlite v1.6.0
   - gorm.io/gorm v1.31.1

Note: The existing go.mod.tmpl may have different content. Replace it entirely with the new pattern. Read the file first if it exists.
  </action>
  <verify>
cat cmd/gux/templates/go.mod.tmpl | grep -E "(gorm\.io/gorm|gorm\.io/driver/sqlite|GuxModule)"
  </verify>
  <done>
go.mod.tmpl includes gux, gorm, and sqlite dependencies with correct versions
  </done>
</task>

<task type="auto">
  <name>Task 3: Create models/item.go.tmpl template</name>
  <files>cmd/gux/templates/models/item.go.tmpl</files>
  <action>
Create the model template at cmd/gux/templates/models/item.go.tmpl.

The template must:
1. Use package models
2. Import gorm.io/gorm
3. Define Item struct that embeds gorm.Model
4. Include fields: Name string with json:"name" tag, Description string with json:"description" tag
5. Add a doc comment explaining this is an example model

The gorm.Model embed provides: ID uint, CreatedAt, UpdatedAt, DeletedAt (soft delete).
  </action>
  <verify>
cat cmd/gux/templates/models/item.go.tmpl | grep -E "(gorm\.Model|Name.*string|Description.*string)"
  </verify>
  <done>
models/item.go.tmpl defines GORM model with embedded gorm.Model and Name/Description fields
  </done>
</task>

</tasks>

<verification>
All three templates exist and contain the required patterns:

```bash
# Verify app.go.tmpl
grep -q "core.New()" cmd/gux/templates/app.go.tmpl && echo "app.go.tmpl: OK"

# Verify go.mod.tmpl
grep -q "gorm.io/gorm" cmd/gux/templates/go.mod.tmpl && echo "go.mod.tmpl: OK"

# Verify models/item.go.tmpl
grep -q "gorm.Model" cmd/gux/templates/models/item.go.tmpl && echo "models/item.go.tmpl: OK"
```
</verification>

<success_criteria>
- [ ] app.go.tmpl creates valid app using core.New(), SetDB(), CRUD(), Routes(), Run()
- [ ] go.mod.tmpl includes gux, gorm, and sqlite dependencies
- [ ] models/item.go.tmpl defines Item with gorm.Model, Name, Description
- [ ] All templates use {{.ModulePath}}, {{.AppName}}, {{.GuxModule}}, {{.GuxVersion}} placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/27-gux-init-templates/27-01-SUMMARY.md`
</output>
