---
phase: 22-saas-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/saas/app.go
  - examples/saas/models/project.go
  - examples/saas/dto/project.go
  - examples/saas/pages/layout.go
autonomous: true

must_haves:
  truths:
    - "User can navigate between dashboard pages using horizontal nav"
    - "Project resource model exists with CRUD operations"
    - "API client is generated for Project operations"
  artifacts:
    - path: "examples/saas/app.go"
      provides: "Route setup and CRUD registration"
      exports: ["main"]
      min_lines: 50
    - path: "examples/saas/models/project.go"
      provides: "Project GORM model"
      contains: "type Project struct"
    - path: "examples/saas/dto/project.go"
      provides: "ProjectList and ProjectDetail DTOs"
      contains: ["ProjectList", "ProjectDetail"]
    - path: "examples/saas/pages/layout.go"
      provides: "DashboardLayout and Nav components"
      exports: ["DashboardLayout", "Nav"]
  key_links:
    - from: "examples/saas/app.go"
      to: "examples/saas/models/project.go"
      via: "CRUD registration"
      pattern: "app\\.CRUD\\(models\\.Project"
    - from: "examples/saas/pages/layout.go"
      to: "core"
      via: "Node rendering"
      pattern: "core\\.Node"
---

<objective>
Build the foundation for the SaaS Dashboard example: Project model, DTOs, CRUD registration, routes, and shared layout components.

Purpose: Establish the data layer and navigation structure that all other pages will use.
Output: Working app.go with routes, Project model with CRUD, DashboardLayout and Nav components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-saas-dashboard/22-RESEARCH.md

# Reference patterns
@examples/minimal/app.go
@examples/minimal/models/user.go
@examples/minimal/dto/user.go
@examples/minimal/admin/nav.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Project model and DTOs</name>
  <files>
    examples/saas/models/project.go
    examples/saas/dto/project.go
  </files>
  <action>
Create the Project model in `examples/saas/models/project.go`:
- Use gorm.Model for ID, CreatedAt, UpdatedAt, DeletedAt
- Fields: Name (string), Description (string), Status (string, default "active")
- Status options: "active", "completed", "archived"

Create DTOs in `examples/saas/dto/project.go`:
- ProjectList DTO: ID, Name, Status, CreatedAt (with gux tags for field mapping)
- ProjectDetail DTO: ID, Name, Description, Status, CreatedAt, UpdatedAt (with gux tags)

Follow the exact pattern from `examples/minimal/dto/user.go` for gux tag syntax.
  </action>
  <verify>
Files exist and compile:
```bash
cd examples/saas && go build ./models/... ./dto/...
```
  </verify>
  <done>
Project model exists with Name, Description, Status fields.
DTOs exist with proper gux tags for CRUD field mapping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app.go with routes and CRUD</name>
  <files>examples/saas/app.go</files>
  <action>
Create `examples/saas/app.go` following the pattern from `examples/minimal/app.go`:

1. Import paths:
   - github.com/dougbarrett/gux/core
   - github.com/dougbarrett/gux/examples/saas/models
   - github.com/dougbarrett/gux/examples/saas/dto
   - github.com/dougbarrett/gux/examples/saas/pages
   - github.com/dougbarrett/gux/examples/saas/.gux/api
   - gorm.io/driver/sqlite
   - gorm.io/gorm

2. Main function:
   - Open SQLite database "app.db"
   - AutoMigrate Project model
   - Seed 3 sample projects if none exist:
     - "Website Redesign" (active)
     - "Mobile App" (active)
     - "API Integration" (completed)
   - Initialize api.Init(db)
   - Create app with core.New()
   - Set title "SaaS Dashboard"

3. Register CRUD for Project:
   - WithListDTO(dto.ProjectList{})
   - WithDetailDTO(dto.ProjectDetail{})

4. Register routes (single bundle, no route groups):
   - Hybrid("/", pages.Dashboard)
   - Hybrid("/projects", pages.Projects)
   - Hybrid("/projects/new", pages.ProjectNew)
   - Hybrid("/projects/:id", pages.ProjectDetail)
   - Hybrid("/projects/:id/edit", pages.ProjectEdit)
   - Hybrid("/settings", pages.Settings)
   - Hybrid("/profile", pages.Profile)

5. Run on port :8082 (to avoid conflicts with other examples)

Note: Pages will be implemented in later plans. Create placeholder functions for now in pages/ if needed for compilation.
  </action>
  <verify>
```bash
cd examples/saas && go build .
```
  </verify>
  <done>
app.go exists with Project CRUD, routes, and database setup.
Application compiles (pages may be placeholders).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardLayout and Nav components</name>
  <files>examples/saas/pages/layout.go</files>
  <action>
Create `examples/saas/pages/layout.go` with shared layout components:

1. DashboardLayout function:
   - Parameters: children ...core.Node
   - Returns: core.Node
   - Structure: min-h-screen bg-gray-900 wrapper
   - Contains: Nav() at top, main content area with max-w-6xl mx-auto px-4 py-8

2. Nav function:
   - Returns: core.Node
   - Follow pattern from `examples/minimal/admin/nav.go` (horizontal nav, NOT sidebar)
   - Dark theme: bg-gray-800, border-b border-gray-700
   - Container: max-w-6xl mx-auto px-4
   - Flex row with justify-between items-center py-4

   Left side (flex items-center gap-6):
   - Brand: "SaaS App" in text-xl font-bold text-white
   - Nav links: Dashboard (/), Projects (/projects), Settings (/settings)
   - Link styling: text-gray-300 hover:text-white transition

   Right side:
   - Use ui.Avatar with Name prop for initials (import from github.com/dougbarrett/gux/ui)
   - Profile link (/profile): text-gray-300 hover:text-white
   - User name: "John Doe" (mock data)

Import ui package for Avatar: github.com/dougbarrett/gux/ui
  </action>
  <verify>
```bash
cd examples/saas && go build ./pages/...
```
Layout components compile and use ui.Avatar correctly.
  </verify>
  <done>
DashboardLayout wraps content with dark theme and Nav.
Nav displays horizontal navigation with brand, links, and user avatar.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Directory structure exists: examples/saas/{app.go, models/, dto/, pages/}
2. `go build ./...` succeeds in examples/saas/
3. Run `gux gen` in examples/saas/ to generate API client
4. Project model has CRUD operations available
5. Nav uses horizontal layout (not sidebar)
</verification>

<success_criteria>
- examples/saas/app.go compiles with routes and CRUD registration
- examples/saas/models/project.go defines Project with gorm.Model
- examples/saas/dto/project.go defines ProjectList and ProjectDetail with gux tags
- examples/saas/pages/layout.go exports DashboardLayout and Nav
- Navigation uses horizontal pattern with ui.Avatar
- All files compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-saas-dashboard/22-01-SUMMARY.md`
</output>
