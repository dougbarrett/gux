---
phase: 12-readme-update
plan: 01
type: execute
---

<objective>
Update README.md with v1.0 and v1.1 features including header components, keyboard shortcuts, accessibility features, and new components.

Purpose: Ensure README accurately reflects current application capabilities so new users understand the full feature set.
Output: Comprehensive README.md covering all v1.0 and v1.1 features with code examples.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/milestones/v1.1-ROADMAP.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.0 Feature Documentation</name>
  <files>README.md</files>
  <action>
Update README.md with v1.0 features:

1. **Update Features list** at top to mention:
   - Command Palette (Cmd+K)
   - Data Export (CSV, JSON, PDF)
   - PWA Support
   - Notification Center

2. **Add Header Components section** after existing component sections:
```go
// UserMenu with avatar and dropdown
userMenu := components.UserMenu(components.UserMenuProps{
    Name:      "John Doe",
    Email:     "john@example.com",
    AvatarURL: "/avatar.png",
    OnLogout:  func() { /* handle logout */ },
})

// NotificationCenter with real-time updates
notifications := components.NotificationCenter(components.NotificationCenterProps{
    Notifications: notificationList,
    OnMarkRead:    func(id string) { /* mark read */ },
    OnClear:       func() { /* clear all */ },
})
```

3. **Add Command Palette section:**
```go
// Command Palette (Cmd/Ctrl+K)
palette := components.CommandPalette(components.CommandPaletteProps{
    Commands: []components.CommandItem{
        {ID: "new", Label: "Create New Post", Category: "Actions", Action: handleNew},
        {ID: "search", Label: "Search", Category: "Navigation", Action: openSearch},
    },
})
```

4. **Add Data Export section:**
```go
// Export table data to CSV, JSON, or PDF
exporter := components.DataExport(components.DataExportProps{
    Data:     tableData,
    Columns:  []string{"Name", "Email", "Status"},
    Filename: "users",
    Formats:  []string{"csv", "json", "pdf"},
})
```

5. **Update Full Component List table** - add new components:
   - Header: UserMenu, NotificationCenter, ConnectionStatus
   - Navigation: CommandPalette
   - Data: DataExport
   - Feedback: EmptyState

6. **Add PWA Support section** after Deployment:
   - Installable web app
   - Offline support with service worker
   - Asset caching strategy
  </action>
  <verify>grep -E "(UserMenu|NotificationCenter|CommandPalette|DataExport)" README.md returns matches for all four</verify>
  <done>README contains v1.0 features: UserMenu, NotificationCenter, CommandPalette, Data Export, PWA sections with code examples</done>
</task>

<task type="auto">
  <name>Task 2: Add v1.1 Accessibility & Keyboard Documentation</name>
  <files>README.md</files>
  <action>
Add accessibility features and keyboard shortcuts to README.md:

1. **Add Accessibility section** (new major section after State Management or before Deployment):
```markdown
## Accessibility

Gux components are built with WCAG 2.1 AA compliance:

- **Screen Reader Support** — All components have proper ARIA labels, roles, and live regions
- **Keyboard Navigation** — Full keyboard access with visible focus indicators
- **Reduced Motion** — Respects `prefers-reduced-motion` system setting
- **Color Contrast** — Meets WCAG 2.1 AA contrast requirements (4.5:1 minimum)
- **Focus Management** — Modal focus traps, focus restoration, skip links

### Accessibility Testing

Automated accessibility testing with axe-core and Playwright:

```bash
make test-a11y        # Run accessibility tests
make test-a11y-debug  # Run with visible browser
```
```

2. **Add Keyboard Shortcuts section** (after Accessibility):
```markdown
## Keyboard Shortcuts

| Shortcut | Action |
|----------|--------|
| `Cmd/Ctrl + K` | Open command palette |
| `Cmd/Ctrl + B` | Toggle sidebar |
| `Escape` | Close modal/dropdown/palette |
| `Enter` | Confirm selection |
| `Arrow Up/Down` | Navigate dropdown/menu items |
| `Arrow Left/Right` | Switch tabs |
| `Home/End` | Jump to first/last tab |
| `Tab` | Move between focusable elements |
```

3. **Update Features bullet at top** to add:
   - WCAG 2.1 AA Accessible

4. **Add FocusTrap to Utilities row** in Full Component List table
  </action>
  <verify>grep -E "(WCAG|Keyboard Shortcuts|prefers-reduced-motion|axe-core)" README.md returns matches</verify>
  <done>README contains: Accessibility section with WCAG compliance, keyboard shortcuts table, testing instructions, reduced motion mention</done>
</task>

<task type="auto">
  <name>Task 3: Update Documentation Links and Polish</name>
  <files>README.md</files>
  <action>
Final polish pass on README:

1. **Update Documentation table** - add new docs (these will be created in later phases):
   - [Keyboard Shortcuts](docs/keyboard-shortcuts.md) | Complete keyboard navigation reference
   - [Accessibility](docs/accessibility.md) | ARIA patterns and a11y guidelines

2. **Update Features section** at top to be more comprehensive:
   - **Type-Safe API Generation** — unchanged
   - **45+ UI Components** — unchanged
   - **WCAG 2.1 AA Accessible** — NEW: Screen reader support, keyboard navigation, focus management
   - **Command Palette** — NEW: Quick actions with Cmd/Ctrl+K
   - **Reactive State Management** — unchanged
   - **Data Export** — NEW: CSV, JSON, and PDF export for tables
   - **WebSocket Support** — unchanged
   - **PWA Ready** — NEW: Installable with offline support
   - **Server Utilities** — unchanged

3. **Ensure consistent formatting**:
   - All code examples use proper Go syntax highlighting
   - Section headings are consistent (## for major, ### for sub)
   - Code comments explain the purpose

4. **Add ConnectionStatus component example** in appropriate section:
```go
// Connection status indicator for WebSocket state
status := components.ConnectionStatus(components.ConnectionStatusProps{
    Connected: wsClient.IsConnected(),
    Variant:   components.StatusDot,  // or StatusBanner
})
```
  </action>
  <verify>Scan README for broken markdown, check Features section has all 9 bullets, verify docs table has 9 entries</verify>
  <done>README has polished Features list (9 items), complete Documentation table (9 links), ConnectionStatus example, consistent formatting</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] README contains UserMenu, NotificationCenter, CommandPalette, DataExport examples
- [ ] README has Accessibility section with WCAG 2.1 AA mention
- [ ] README has Keyboard Shortcuts table
- [ ] README Features list updated with all v1.0/v1.1 features
- [ ] Full Component List table includes new components
- [ ] Documentation table includes new doc links
- [ ] All code examples have Go syntax highlighting
</verification>

<success_criteria>

- All tasks completed
- README accurately describes current application capabilities
- New users can discover v1.0 and v1.1 features from README
- Keyboard shortcuts are documented in a clear table
- Accessibility features are prominently documented
</success_criteria>

<output>
After completion, create `.planning/phases/12-readme-update/12-01-SUMMARY.md`:

# Phase 12 Plan 01: README Update Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `README.md` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for Phase 13: Component Docs
</output>
