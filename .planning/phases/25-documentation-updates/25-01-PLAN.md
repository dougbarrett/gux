---
phase: 25-documentation-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/_sidebar.md
  - CLAUDE.md
  - LLM.txt
autonomous: true

must_haves:
  truths:
    - "Docs navigation has no broken links to deleted files"
    - "CLAUDE.md project structure lists only existing packages"
    - "LLM.txt repository structure lists only existing packages"
  artifacts:
    - path: "docs/_sidebar.md"
      provides: "Documentation navigation"
      contains: "Server Utilities"
      not_contains: ["components.md", "state-management.md", "websocket.md", "auth.md"]
    - path: "CLAUDE.md"
      provides: "Project instructions"
      not_contains: ["components/", "state/", "auth/", "storage/", "ws/"]
    - path: "LLM.txt"
      provides: "LLM context file"
      not_contains: ["├── auth/", "├── components/", "├── state/", "├── storage/", "├── ws/"]
  key_links:
    - from: "docs/_sidebar.md"
      to: "existing doc files only"
      via: "markdown links"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
---

<objective>
Remove documentation files for deleted packages and update navigation and core documentation.

Purpose: Eliminate stale references to packages removed in Phase 24 (components/, auth/, storage/, state/, ws/)
Output: Cleaned documentation with no broken links or dead package references in navigation and core docs
</objective>

<execution_context>
@/Users/dougbarrett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dougbarrett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-documentation-updates/25-CONTEXT.md
@.planning/phases/25-documentation-updates/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead documentation files and update navigation</name>
  <files>
    docs/websocket.md (DELETE)
    docs/auth.md (DELETE)
    docs/components.md (DELETE)
    docs/state-management.md (DELETE)
    COMPONENTS.md (DELETE)
    docs/_sidebar.md (EDIT)
  </files>
  <action>
Delete these 5 files entirely (they only document dead packages):
- docs/websocket.md (575 lines, documents ws/ package)
- docs/auth.md (474 lines, documents auth/ package)
- docs/components.md (1364 lines, documents components/ package)
- docs/state-management.md (568 lines, documents state/ package)
- COMPONENTS.md (1027 lines, documents components/ and state/ packages)

Then edit docs/_sidebar.md to remove navigation entries for deleted files:
- Remove `- [Components](components.md)` from Core Concepts section
- Remove `- [State Management](state-management.md)` from Core Concepts section
- Remove `- [WebSocket](websocket.md)` from Features section
- Remove `- [Authentication](auth.md)` from Features section

The resulting _sidebar.md should have:
- Getting Started section: Introduction, Getting Started, CLI Reference
- Core Concepts section: API Generation, Templates (only)
- Features section: Server Utilities (only)
- Reference section: Keyboard Shortcuts, Accessibility, Deployment
  </action>
  <verify>
    - Deleted files no longer exist: `ls docs/websocket.md docs/auth.md docs/components.md docs/state-management.md COMPONENTS.md 2>&1` shows "No such file"
    - _sidebar.md has no references to deleted files: `grep -E "(components|state-management|websocket|auth)\.md" docs/_sidebar.md` returns no matches
  </verify>
  <done>
    - 5 documentation files deleted
    - _sidebar.md contains only links to existing documentation files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and LLM.txt project structure</name>
  <files>
    CLAUDE.md
    LLM.txt
  </files>
  <action>
Edit CLAUDE.md:
- Find the "Project Structure" section (tree diagram)
- Remove these lines from the tree:
  - `├── components/             # Pre-built UI components (Gux library)` (if present)
  - `├── state/                  # State management utilities` (if present)
  - Any other lines referencing components/, auth/, storage/, state/, ws/
- Leave the structure intact otherwise (core/, fetch/, api/, server/, cmd/gux/, examples/)

Edit LLM.txt:
- Find the "Repository Structure" section (tree diagram)
- Remove these lines from the tree:
  - `├── auth/             # WASM: JWT parsing, auth state, role-based access`
  - `├── components/       # WASM: 45+ UI components (buttons, forms, charts, etc.)`
  - `├── state/            # WASM: Reactive stores, async state, query caching`
  - `├── storage/          # WASM: localStorage/sessionStorage access`
  - `├── ws/               # WASM: Type-safe WebSocket client`
- Update "Key Concepts > Build Targets" section - remove mention of (auth, components, fetch, state, storage, ws), keep only (fetch)
- Update "Important Files" table - remove rows for:
  - `components/app.go`
  - `state/store.go`
  - `state/querycache.go`
  - `ws/ws.go`
  - `auth/auth.go`
- Update "State Management" section - remove entirely (or note state is no longer a separate package)
- Update "Components Pattern" section - remove entirely (or note components is no longer a separate package)
- Remove any other references to dead packages in prose
- Update project overview if it mentions "45+ production-ready UI components" or similar claims about dead packages
  </action>
  <verify>
    - No dead package references in CLAUDE.md: `grep -E "(components/|state/|auth/|storage/|ws/)" CLAUDE.md` returns no matches
    - No dead package references in LLM.txt: `grep -E "├── (auth|components|state|storage|ws)/" LLM.txt` returns no matches
  </verify>
  <done>
    - CLAUDE.md project structure shows only existing packages
    - LLM.txt repository structure and content references only existing packages
  </done>
</task>

</tasks>

<verification>
Phase 25 Plan 01 verification:
1. File deletions: `ls docs/websocket.md docs/auth.md docs/components.md docs/state-management.md COMPONENTS.md 2>&1 | grep -c "No such file"` should equal 5
2. Navigation clean: `grep -E "(components|state-management|websocket|auth)\.md" docs/_sidebar.md` returns no output
3. CLAUDE.md clean: `grep -E "(components/|state/|auth/|storage/|ws/)" CLAUDE.md` returns no output
4. LLM.txt structure clean: `grep -E "├── (auth|components|state|storage|ws)/" LLM.txt` returns no output
</verification>

<success_criteria>
- 5 documentation files deleted (docs/websocket.md, docs/auth.md, docs/components.md, docs/state-management.md, COMPONENTS.md)
- docs/_sidebar.md contains no links to deleted files
- CLAUDE.md has no references to dead packages in project structure
- LLM.txt has no references to dead packages in repository structure, important files, or examples
</success_criteria>

<output>
After completion, create `.planning/phases/25-documentation-updates/25-01-SUMMARY.md`
</output>
