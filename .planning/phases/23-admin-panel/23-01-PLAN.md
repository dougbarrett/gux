---
phase: 23-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/admin/app.go
  - examples/admin/models/user.go
  - examples/admin/models/activity_log.go
  - examples/admin/dto/user.go
  - examples/admin/dto/activity_log.go
  - examples/admin/pages/layout.go
autonomous: true

must_haves:
  truths:
    - "Admin app starts on port 8084"
    - "User and ActivityLog models exist with extended fields"
    - "DTOs exclude sensitive fields (PasswordHash)"
    - "Nav displays brand and navigation links"
    - "AdminLayout wraps content with dark theme"
  artifacts:
    - path: "examples/admin/app.go"
      provides: "App setup, routes, CRUD, seeding"
      exports: ["main"]
    - path: "examples/admin/models/user.go"
      provides: "User model with Role, Status, LastLoginAt"
      contains: "type User struct"
    - path: "examples/admin/models/activity_log.go"
      provides: "ActivityLog model for audit trail"
      contains: "type ActivityLog struct"
    - path: "examples/admin/dto/user.go"
      provides: "UserList, UserDetail DTOs"
      contains: "type UserList struct"
    - path: "examples/admin/dto/activity_log.go"
      provides: "ActivityLogList DTO"
      contains: "type ActivityLogList struct"
    - path: "examples/admin/pages/layout.go"
      provides: "AdminLayout, Nav components"
      exports: ["AdminLayout", "Nav"]
  key_links:
    - from: "examples/admin/app.go"
      to: "examples/admin/models"
      via: "import and AutoMigrate"
      pattern: "db\\.AutoMigrate.*User.*ActivityLog"
    - from: "examples/admin/app.go"
      to: "examples/admin/pages"
      via: "route registration"
      pattern: "Hybrid.*pages\\."
---

<objective>
Create the Admin Panel example foundation with models, DTOs, routes, and layout.

Purpose: Establish the core structure for the admin panel example, including User model with Role/Status fields, ActivityLog model for audit trail, DTOs for secure API responses, and the AdminLayout with horizontal navigation.

Output: A working admin app skeleton that starts on port 8084 with seeded data and basic navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-admin-panel/23-RESEARCH.md

Reference implementations:
@examples/saas/app.go
@examples/saas/pages/layout.go
@examples/minimal/models/user.go
@examples/minimal/dto/user.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Models (User, ActivityLog)</name>
  <files>
    examples/admin/models/user.go
    examples/admin/models/activity_log.go
  </files>
  <action>
Create the models directory and two model files:

**models/user.go:**
- User struct with gorm.Model embedding
- Fields: Email (uniqueIndex), Name, PasswordHash (json:"-" to never expose), Role (default "user"), Status (default "active"), LastLoginAt (*time.Time, nullable)
- SetPassword method using bcrypt (same pattern as examples/minimal/models/user.go)
- CheckPassword method for verification

**models/activity_log.go:**
- ActivityLog struct with gorm.Model embedding
- Fields: UserID (*uint, nullable for system events), User (*User, gorm foreignKey), Action (string: created, updated, deleted, login, logout), Entity (string: user, setting), EntityID (*uint, nullable), Description (string), IPAddress (string, omitempty), Metadata (string, omitempty for JSON)

Import golang.org/x/crypto/bcrypt for password hashing.
  </action>
  <verify>
    go build ./examples/admin/models/...
  </verify>
  <done>
    Both model files compile without errors. User has Role/Status/LastLoginAt fields. ActivityLog has UserID/Action/Entity/Description fields. PasswordHash uses json:"-" tag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create DTOs (UserList, UserDetail, ActivityLogList)</name>
  <files>
    examples/admin/dto/user.go
    examples/admin/dto/activity_log.go
  </files>
  <action>
Create the dto directory and two DTO files:

**dto/user.go:**
- UserList struct: ID, Email, Name, Role, Status, LastLoginAt (*time.Time), CreatedAt - all with json and gux tags
- UserDetail struct: Same as UserList plus UpdatedAt
- UserCreate struct: Email, Name, Password (plaintext), Role, Status
- UserUpdate struct: ID, Email, Name, Password (omitempty), Role, Status

**dto/activity_log.go:**
- ActivityLogList struct: ID, UserID (*uint), UserName (string, derived), Action, Entity, EntityID (*uint), Description, CreatedAt - with json and gux tags

Import time package. Use gux:"Model.Field" tags for field mapping. Note: UserName in ActivityLogList will be populated via preload pattern.
  </action>
  <verify>
    go build ./examples/admin/dto/...
  </verify>
  <done>
    Both DTO files compile. UserList/UserDetail exclude PasswordHash. ActivityLogList includes UserName for display.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Layout, Routes, and App Entry</name>
  <files>
    examples/admin/pages/layout.go
    examples/admin/app.go
  </files>
  <action>
Create pages directory and layout, then main app file:

**pages/layout.go:**
- Import core, ui packages
- AdminLayout function: takes children ...core.Node, returns dark theme wrapper (min-h-screen bg-gray-900) with Nav() and main content area (max-w-6xl mx-auto px-4 py-8)
- Nav function: horizontal nav bar (bg-gray-800 border-b border-gray-700) with:
  - Left side: Brand "Admin Panel" + links (Dashboard /, Users /users, Activity /activity, Settings /settings)
  - Right side: Avatar with initials "AU" + "Admin User" text link
- Use ui.Avatar with Name="Admin User", Size=ui.AvatarSM
- All nav links use text-gray-300 hover:text-white transition

**app.go:**
- Package main with imports (core, models, dto, pages, gorm, sqlite)
- main function:
  - Open SQLite database "admin.db"
  - AutoMigrate User and ActivityLog models
  - Seed data if empty: 1 admin user (admin@example.com, "Admin User", role=admin, status=active), 3 regular users with varied roles/statuses, 5 sample activity logs
  - api.Init(db) for generated API
  - Create app with core.New(), SetDB, SetTitle("Admin Panel")
  - Register CRUD for User with WithListDTO(dto.UserList{}), WithDetailDTO(dto.UserDetail{})
  - Register CRUD for ActivityLog with WithListDTO(dto.ActivityLogList{})
  - Stub routes (pages.Dashboard, pages.Users, pages.UserDetail, pages.UserEdit, pages.UserNew, pages.Activity, pages.Settings) - create stub page functions that return AdminLayout with placeholder text
  - app.Run(":8084")

For stub pages, create minimal functions in layout.go temporarily:
```go
func Dashboard(r *core.Router) func() core.Node {
    return func() core.Node {
        return AdminLayout(core.Text("Dashboard - Coming Soon"))
    }
}
// Similar for Users, UserDetail, UserEdit, UserNew, Activity, Settings
```
  </action>
  <verify>
    go build ./examples/admin && ls examples/admin/app.go examples/admin/pages/layout.go
  </verify>
  <done>
    App builds successfully. AdminLayout renders dark theme with Nav. Routes registered for all pages. Port 8084 configured. Seed data creates admin user and sample activity logs.
  </done>
</task>

</tasks>

<verification>
1. `go build ./examples/admin/...` - All packages compile
2. `ls examples/admin/models/` - user.go, activity_log.go exist
3. `ls examples/admin/dto/` - user.go, activity_log.go exist
4. `grep -r "json:\"-\"" examples/admin/models/` - PasswordHash is hidden
5. `grep "8084" examples/admin/app.go` - Port configured correctly
</verification>

<success_criteria>
- Admin example directory structure created (models/, dto/, pages/)
- User model has Role, Status, LastLoginAt fields
- ActivityLog model has UserID, Action, Entity, Description fields
- DTOs exclude PasswordHash (uses json:"-")
- AdminLayout provides dark theme wrapper with horizontal Nav
- App runs on port 8084 with seeded users and activity logs
- All stub routes registered and return placeholder content
</success_criteria>

<output>
After completion, create `.planning/phases/23-admin-panel/23-01-SUMMARY.md`
</output>
