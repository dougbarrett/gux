---
phase: 18-data-display-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/table.go
  - ui/table_test.go
  - ui/list.go
  - ui/list_test.go
autonomous: true

must_haves:
  truths:
    - "Table compound components render correct HTML structure"
    - "Table includes overflow wrapper for wide content"
    - "List renders items with consistent styling"
    - "Compound components compose naturally"
  artifacts:
    - path: "ui/table.go"
      provides: "Table compound components"
      exports: ["Table", "TableProps", "Thead", "TheadProps", "Tbody", "TbodyProps", "Tr", "TrProps", "Th", "ThProps", "Td", "TdProps"]
    - path: "ui/table_test.go"
      provides: "Table test coverage"
      contains: "TestTable"
    - path: "ui/list.go"
      provides: "List compound components"
      exports: ["List", "ListProps", "ListItem", "ListItemProps"]
    - path: "ui/list_test.go"
      provides: "List test coverage"
      contains: "TestList"
  key_links:
    - from: "ui/table.go"
      to: "core/elements.go"
      via: "core.Table, core.Thead, core.Tbody, core.Tr, core.Th, core.Td"
      pattern: "core\\.Table|core\\.Thead|core\\.Tbody"
    - from: "ui/list.go"
      to: "core/elements.go"
      via: "core.Ul, core.Li"
      pattern: "core\\.Ul|core\\.Li"
---

<objective>
Create Table compound components and List component following Card's compound pattern.

Purpose: Structured data display requires composable table and list components.
Output: Table/Thead/Tbody/Tr/Th/Td compound pattern, List/ListItem pattern, full test coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-display-components/18-RESEARCH.md

Reference patterns:
@ui/card.go (compound component pattern - follow this exactly)
@ui/button.go (props struct pattern)
@ui/utils.go (MergeClasses)
@core/elements.go (Table, Thead, Tbody, Tr, Th, Td, Ul, Li helpers exist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Table compound components</name>
  <files>ui/table.go, ui/table_test.go</files>
  <action>
Create Table compound components following Card's pattern exactly:

1. Define props structs in ui/table.go:
   ```go
   type TableProps struct {
       Class    string
       Children []core.Node
   }

   type TheadProps struct {
       Class    string
       Children []core.Node
   }

   type TbodyProps struct {
       Class    string
       Children []core.Node
   }

   type TrProps struct {
       Class    string
       OnClick  func()      // Row click handler (WASM only)
       Children []core.Node
   }

   type ThProps struct {
       Class    string
       Children []core.Node
   }

   type TdProps struct {
       Class    string
       Children []core.Node
   }
   ```

2. Implement Table function with overflow wrapper:
   ```go
   func Table(props TableProps) core.Node {
       tableClass := MergeClasses(
           "min-w-full divide-y divide-gray-200 dark:divide-gray-700",
           props.Class,
       )
       // Wrap in overflow-x-auto div to prevent wide tables breaking layout
       return core.Div(core.Class("overflow-x-auto"),
           core.Table(core.Class(tableClass), props.Children...),
       )
   }
   ```

3. Implement Thead:
   ```go
   func Thead(props TheadProps) core.Node {
       class := MergeClasses(
           "bg-gray-50 dark:bg-gray-800",
           props.Class,
       )
       return core.Thead(core.Class(class), props.Children...)
   }
   ```

4. Implement Tbody:
   ```go
   func Tbody(props TbodyProps) core.Node {
       class := MergeClasses(
           "bg-white dark:bg-gray-900 divide-y divide-gray-200 dark:divide-gray-700",
           props.Class,
       )
       return core.Tbody(core.Class(class), props.Children...)
   }
   ```

5. Implement Tr with optional OnClick:
   ```go
   func Tr(props TrProps) core.Node {
       attrs := core.Attrs{
           Class:   MergeClasses(props.Class),
           OnClick: props.OnClick,
       }
       return core.Tr(attrs, props.Children...)
   }
   ```

6. Implement Th:
   ```go
   func Th(props ThProps) core.Node {
       class := MergeClasses(
           "px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider",
           props.Class,
       )
       return core.Th(core.Class(class), props.Children...)
   }
   ```

7. Implement Td:
   ```go
   func Td(props TdProps) core.Node {
       class := MergeClasses(
           "px-6 py-4 whitespace-nowrap text-sm text-gray-900 dark:text-gray-100",
           props.Class,
       )
       return core.Td(core.Class(class), props.Children...)
   }
   ```

8. Create ui/table_test.go with tests:
   - TestTable_BasicStructure: Table renders table element with children
   - TestTable_OverflowWrapper: Table is wrapped in overflow-x-auto div
   - TestThead_Styling: Thead has correct background
   - TestTbody_Styling: Tbody has correct styling
   - TestTr_CustomClass: Tr accepts custom classes
   - TestTh_Styling: Th has header styling
   - TestTd_Styling: Td has cell styling
   - TestTable_Compound: Full table with all parts renders correctly
  </action>
  <verify>cd /Users/dougbarrett/projects/dbb1dev/goquery && go test ./ui/... -run TestTable -v && go test ./ui/... -run TestThead -v && go test ./ui/... -run TestTbody -v && go test ./ui/... -run TestTr -v && go test ./ui/... -run TestTh -v && go test ./ui/... -run TestTd -v</verify>
  <done>Table compound components render correct HTML, include overflow wrapper, all parts have appropriate styling</done>
</task>

<task type="auto">
  <name>Task 2: List compound components</name>
  <files>ui/list.go, ui/list_test.go</files>
  <action>
Create List compound components:

1. Define props structs in ui/list.go:
   ```go
   type ListProps struct {
       Class    string
       Children []core.Node
   }

   type ListItemProps struct {
       Class    string
       OnClick  func()      // Item click handler (WASM only)
       Children []core.Node
   }
   ```

2. Implement List function:
   ```go
   func List(props ListProps) core.Node {
       class := MergeClasses(
           "divide-y divide-gray-200 dark:divide-gray-700",
           props.Class,
       )
       return core.Ul(core.Class(class), props.Children...)
   }
   ```

3. Implement ListItem function:
   ```go
   func ListItem(props ListItemProps) core.Node {
       class := MergeClasses(
           "px-4 py-4 hover:bg-gray-50 dark:hover:bg-gray-800",
           props.Class,
       )
       attrs := core.Attrs{
           Class:   class,
           OnClick: props.OnClick,
       }
       return core.Li(attrs, props.Children...)
   }
   ```

4. Create ui/list_test.go with tests:
   - TestList_BasicStructure: List renders ul element
   - TestList_Dividers: List has divide styling
   - TestListItem_BasicStructure: ListItem renders li element
   - TestListItem_Hover: ListItem has hover styling
   - TestListItem_CustomClass: Custom classes applied
   - TestList_Compound: Full list with items renders correctly
  </action>
  <verify>cd /Users/dougbarrett/projects/dbb1dev/goquery && go test ./ui/... -run TestList -v</verify>
  <done>List and ListItem render correct HTML with dividers and hover states</done>
</task>

</tasks>

<verification>
Run full ui package tests to ensure no regressions:
```bash
cd /Users/dougbarrett/projects/dbb1dev/goquery && go test ./ui/... -v
```

Verify exports are available:
```bash
cd /Users/dougbarrett/projects/dbb1dev/goquery && go build ./ui/...
```
</verification>

<success_criteria>
- Table compound (Table, Thead, Tbody, Tr, Th, Td) renders correct HTML structure
- Table includes overflow-x-auto wrapper for wide content safety
- List compound (List, ListItem) renders correct HTML structure
- All components follow Card's compound pattern exactly
- All tests pass with good coverage
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-display-components/18-02-SUMMARY.md`
</output>
