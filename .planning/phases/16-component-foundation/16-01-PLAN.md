---
phase: 16-component-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/utils.go
  - ui/utils_test.go
autonomous: true

must_haves:
  truths:
    - "mergeClasses combines multiple class strings into one"
    - "Empty strings are filtered from merged output"
    - "conditionalClass returns class when true, empty when false"
  artifacts:
    - path: "ui/utils.go"
      provides: "Class merging and conditional utilities"
      exports: ["mergeClasses", "conditionalClass"]
    - path: "ui/utils_test.go"
      provides: "Unit tests for utilities"
      contains: "TestMergeClasses"
  key_links:
    - from: "ui/utils.go"
      to: "strings"
      via: "stdlib import"
      pattern: 'import.*"strings"'
---

<objective>
Create utility functions for the component library.

Purpose: All components need class merging and conditional attribute helpers. Establishing these first enables consistent patterns across Button, Card, and Layout components.

Output: `ui/utils.go` with `mergeClasses` and `conditionalClass` functions, plus unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-component-foundation/16-RESEARCH.md

# Core package reference
@core/node.go
@core/elements.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ui package with utility functions</name>
  <files>ui/utils.go</files>
  <action>
Create a new `ui/` package at the project root with utility functions:

1. `mergeClasses(classes ...string) string` - Combines CSS class strings:
   - Filter out empty strings (after trimming whitespace)
   - Join remaining strings with single space
   - Example: `mergeClasses("foo", "", "bar")` returns `"foo bar"`

2. `conditionalClass(condition bool, class string) string` - Returns class if true:
   - If condition is true, return the class string
   - If condition is false, return empty string
   - Example: `conditionalClass(true, "hidden")` returns `"hidden"`
   - Example: `conditionalClass(false, "hidden")` returns `""`

Package should be `package ui` with import path `github.com/dougbarrett/gux/ui`.

Implementation reference from research:
```go
package ui

import "strings"

// mergeClasses combines CSS class strings, filtering empty values.
// Custom classes should be passed last to allow overrides.
func mergeClasses(classes ...string) string {
    var result []string
    for _, c := range classes {
        c = strings.TrimSpace(c)
        if c != "" {
            result = append(result, c)
        }
    }
    return strings.Join(result, " ")
}

// conditionalClass returns the class if condition is true, empty string otherwise.
func conditionalClass(condition bool, class string) string {
    if condition {
        return class
    }
    return ""
}
```

Ensure functions are exported (capitalized): `MergeClasses`, `ConditionalClass`.
  </action>
  <verify>
Run: `go build ./ui/...` - should compile without errors
  </verify>
  <done>
`ui/utils.go` exists with exported `MergeClasses` and `ConditionalClass` functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for utilities</name>
  <files>ui/utils_test.go</files>
  <action>
Create `ui/utils_test.go` with comprehensive tests:

1. `TestMergeClasses` - Test cases:
   - Single class: `MergeClasses("foo")` == `"foo"`
   - Multiple classes: `MergeClasses("foo", "bar")` == `"foo bar"`
   - Empty strings filtered: `MergeClasses("foo", "", "bar")` == `"foo bar"`
   - Whitespace trimmed: `MergeClasses("  foo  ", "bar")` == `"foo bar"`
   - All empty returns empty: `MergeClasses("", "")` == `""`
   - No args returns empty: `MergeClasses()` == `""`

2. `TestConditionalClass` - Test cases:
   - True returns class: `ConditionalClass(true, "hidden")` == `"hidden"`
   - False returns empty: `ConditionalClass(false, "hidden")` == `""`

Use standard Go testing patterns with `t.Run` for subtests.
  </action>
  <verify>
Run: `go test ./ui/... -v` - all tests pass
  </verify>
  <done>
All utility tests pass. Coverage includes edge cases (empty strings, whitespace, no args).
  </done>
</task>

</tasks>

<verification>
1. `go build ./ui/...` compiles successfully
2. `go test ./ui/... -v` passes all tests
3. `MergeClasses` and `ConditionalClass` are exported and documented
</verification>

<success_criteria>
- [ ] `ui/utils.go` exists with `MergeClasses` and `ConditionalClass` functions
- [ ] Functions are exported (capitalized names)
- [ ] Unit tests exist and pass
- [ ] Empty strings and whitespace handled correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-component-foundation/16-01-SUMMARY.md`
</output>
