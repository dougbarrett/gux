---
phase: 26-dependency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
autonomous: true

must_haves:
  truths:
    - "go.mod contains only used dependencies"
    - "gorilla/websocket is removed from go.mod"
    - "go.sum is synchronized with go.mod"
    - "All five examples build successfully"
  artifacts:
    - path: "go.mod"
      provides: "Clean dependency list"
      contains: "!gorilla/websocket"
    - path: "go.sum"
      provides: "Synchronized checksums"
  key_links:
    - from: "go.mod"
      to: "examples/*"
      via: "shared dependencies"
      pattern: "gorm.io|crypto|fsnotify"
---

<objective>
Clean up unused dependencies and verify all example applications build successfully.

Purpose: Complete v2.1 dead code cleanup milestone by removing orphaned dependencies left over from deleted packages (ws/, auth/, etc.) and ensuring the entire codebase builds correctly.

Output: Clean go.mod/go.sum files with only active dependencies, verified builds for all 5 example apps.
</objective>

<execution_context>
@/Users/dougbarrett/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dougbarrett/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-dependency-cleanup/26-RESEARCH.md

# Current go.mod shows gorilla/websocket as only direct dependency (unused)
@go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean dependencies with go mod tidy</name>
  <files>go.mod, go.sum</files>
  <action>
    Run dependency cleanup from repository root:

    1. Run `go mod tidy -v` to remove unused dependencies and sync go.sum
       - This will remove gorilla/websocket (unused since ws/ package deleted)
       - Indirect dependencies become direct where actually imported

    2. Run `go mod verify` to ensure checksums are valid

    3. Run `git diff go.mod` to confirm gorilla/websocket was removed

    IMPORTANT: Run at repository root (/Users/dougbarrett/projects/dbb1dev/goquery), not in examples/
  </action>
  <verify>
    - `go mod tidy` completes without errors
    - `go mod verify` outputs "all modules verified"
    - `grep -c gorilla go.mod` returns 0 (websocket removed)
  </verify>
  <done>go.mod no longer contains gorilla/websocket, go.sum is synchronized</done>
</task>

<task type="auto">
  <name>Task 2: Verify all examples build</name>
  <files>examples/minimal/, examples/auth/, examples/marketing/, examples/admin/, examples/saas/</files>
  <action>
    Build each example application using gux build (NOT go build):

    1. cd examples/minimal && gux build
    2. cd examples/auth && gux build
    3. cd examples/marketing && gux build
    4. cd examples/admin && gux build
    5. cd examples/saas && gux build

    Each build must:
    - Generate WASM files (.gux/dist/*.wasm)
    - Generate server binary (bin/app)
    - Complete without errors

    IMPORTANT: Use `gux build`, not `go build`. Examples use //go:embed for WASM files that gux generates.
  </action>
  <verify>
    - Each example's gux build completes with exit code 0
    - Each example has bin/app binary created
  </verify>
  <done>All 5 examples (minimal, auth, marketing, admin, saas) build successfully</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Dependency verification:
   ```bash
   # No gorilla/websocket
   ! grep -q gorilla go.mod

   # Module verification passes
   go mod verify
   ```

2. Build verification:
   ```bash
   # All examples have binaries
   ls examples/*/bin/app
   # Should show 5 files
   ```

3. Requirements coverage:
   - DEPS-01: gorilla/websocket removed (verified by grep)
   - DEPS-02: go mod tidy completed (go.sum synchronized)
   - DEPS-03: All examples build (5 binaries exist)
</verification>

<success_criteria>
- go.mod contains no reference to gorilla/websocket
- go.sum is synchronized (go mod verify passes)
- All 5 example applications build without errors
- v2.1 milestone dependency cleanup requirements (DEPS-01, DEPS-02, DEPS-03) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/26-dependency-cleanup/26-01-SUMMARY.md`

Commit message format:
```
chore(26): clean up dependencies and verify builds

- Remove unused gorilla/websocket dependency
- Run go mod tidy to sync go.sum
- Verify all 5 examples build successfully

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
```
</output>
