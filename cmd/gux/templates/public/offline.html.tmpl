<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>You're Offline - {{.AppName}}</title>
  <style>
    :root {
      --bg-color: #f9fafb;
      --text-color: #111827;
      --text-secondary: #6b7280;
      --card-bg: #ffffff;
      --card-border: #e5e7eb;
      --btn-bg: #3b82f6;
      --btn-hover: #2563eb;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --bg-color: #111827;
        --text-color: #f9fafb;
        --text-secondary: #9ca3af;
        --card-bg: #1f2937;
        --card-border: #374151;
      }
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: var(--bg-color);
      color: var(--text-color);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .container {
      text-align: center;
      max-width: 400px;
      background: var(--card-bg);
      border: 1px solid var(--card-border);
      border-radius: 1rem;
      padding: 2.5rem 2rem;
    }

    .icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    h1 { font-size: 1.5rem; margin-bottom: 0.75rem; }
    .description { color: var(--text-secondary); margin-bottom: 1.5rem; }

    .retry-button {
      background: var(--btn-bg);
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.2s;
    }
    .retry-button:hover { background: var(--btn-hover); }
  </style>
</head>
<body>
  <main class="container">
    <div class="icon">&#9729;</div>
    <h1>You're Offline</h1>
    <p class="description">{{.AppName}} requires a network connection to load.</p>
    <button class="retry-button" onclick="location.reload()">Try Again</button>
  </main>
</body>
</html>
